export const signup_post = (req: any, res: { send: (arg0: string) => void; }) => {
    res.send('signup');
}

export function login_post(req: any, res: { send: (arg0: string) => void; }) {
    res.send('login')
}